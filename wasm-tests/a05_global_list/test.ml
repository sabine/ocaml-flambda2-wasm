(* 5 blocks 2816 = block length 2, color 11, tag 0·
 *
 * Note: in contrast to array, a list is not copied to the heap, but instead
 * the individual list elements live in the data section of the module.*)
let list_ = [6;7;8;9;10]

